<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Location Permissions (Required for accessing user location) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
        [NEW in API 33+] Notification Runtime Permission:
        Required for sending non-exempt notifications on Android 13 (API 33) and higher.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        [MODERNIZED STORAGE] Legacy Storage Permissions:
        These are constrained with maxSdkVersion="32". For apps targeting Android 13+,
        you should use the MediaStore API or new media permissions (READ_MEDIA_IMAGES, etc.).
        Limiting the maxSdkVersion ensures this permission is ignored on newer Android versions.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:label="Diary B2B Orders"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:requestLegacyExternalStorage="false"> <!-- Modern Android default is Scoped Storage,
        set explicitly to false -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity="com.dairyB2b.orders"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Normal launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Link Handler (dairyB2bOrders://) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="dairyB2bOrders" />
            </intent-filter>

            <!-- Existing meta-data -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
            />

        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!--
        Required to query activities that can process text and discover other apps (Package Visibility).
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>

        <!-- Allow this app to discover dairyB2b Management (Existing deep link query) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="dairyB2bManagement" />
        </intent>
    </queries>

</manifest>